[
  {
    "key": "connectionStringSalto",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Connection string",
      "placeholder": "Data Source=server\\instance;Trusted_Connection=True;Initial Catalog=Salto_RW",
      "description": "The connection string used to connect to the Salto SQL database. Must include Initial Catalog",
      "required": true
    }
  },
  {
    "key": "connectionStringStaging",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Connection string",
      "placeholder": "Data Source=server\\instance;;Trusted_Connection=True;Initial Catalog=SALTO_INTERFACES;",
      "description": "The connection string used to connect to the Staging SQL database. Must include Initial Catalog",
      "required": true
    }
  },
  {
    "key": "username",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Username",
      "placeholder": "sa_helloid",
      "description": "Optional: The username of the SQL user to use in the connection string",
      "required": false
    }
  },
  {
    "key": "password",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Password",
      "type": "password",
      "placeholder": "P@ssw0rd1!",
      "description": "Optional: The username of the SQL user to use in the connection string",
      "required": false
    }
  },
  {
    "key": "DbTableStaging",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Salto staging table name",
      "placeholder": "SaltoCardholdersSQLServer",
      "description": "Please enter the name of the Salto staging table",
      "required": true
    }
  },
  {
    "key": "isDebug",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Toggle debug logging",
      "description": "When toggled, debug logging will be displayed",
      "required": false
    }
  }
]